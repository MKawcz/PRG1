#include <iostream>
#include <string>

auto main(int argc, char* argv[])->int
{
    if (argc == 0) {
        return 1;
    }

    

    auto const liczba = std::string{argv[1]};

    std::stoi(argv[n]);	

    for(auto i=liczba; i>=0; i--)
    {
     std::cout<<i<<"..."<<"\n";
    }

    return 0;
}
std::stoi(argv[n]);
